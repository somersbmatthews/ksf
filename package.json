{
  "name": "ks",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build-ts": "lerna run build-ts",
    "lint": "lerna run lint",
    "postinstall": "npm run bootstrap && npm run build-ts",
    "test": "lerna run test"
  },
  "engines": {
    "node": ">=16 <17",
    "npm": ">=7 <8"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
